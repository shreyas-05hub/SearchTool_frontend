React frontend for Knowledge Discovery project.
Setup:
1. Install dependencies:
   npm install
2. Start the dev server:
   npm start
3. It will run on http://localhost:3000 by default
Note: Backend should be running on http://127.0.0.1:8000
